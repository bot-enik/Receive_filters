function Hd = RRC_quantized(Fs, N, Fc, TM, R, DT, Beta, max_digit, fraction_digit)
%RRC_QUANTIZED Returns a discrete-time filter object.

% MATLAB Code
% Generated by MATLAB(R) 9.4 and DSP System Toolbox 9.6.
% Generated on: 21-Feb-2019 11:43:28

% FIR Window Raised-cosine filter designed using the FIRRCOS function.

% All frequency values are in kHz.
% Fs = 144;  % Sampling Frequency
% 
% N    = 64;         % Order
% Fc   = 9;          % Cutoff Frequency
% TM   = 'Rolloff';  % Transition Mode
% R    = 0.35;       % Rolloff
% DT   = 'sqrt';     % Design Type
% Beta = 0.5;        % Window Parameter

% Create the window vector for the design algorithm.
win = kaiser(N+1, Beta);

% Calculate the coefficients using the FIR1 function.
b  = firrcos(N, Fc/(Fs/2), R, 2, TM, DT, [], win);
Hd = dfilt.dffir(b);
% Set the arithmetic property.
set(Hd, 'Arithmetic', 'fixed', ...
    'CoeffWordLength', max_digit, ...
    'CoeffAutoScale', false, ...
    'NumFracLength', fraction_digit, ...
    'Signed',         true, ...
    'InputWordLength', max_digit, ...
    'inputFracLength', fraction_digit, ...
    'FilterInternals',  'SpecifyPrecision', ...
    'OutputWordLength',  max_digit, ...
    'OutputFracLength',  fraction_digit, ...
    'ProductWordLength', max_digit, ...
    'ProductFracLength', fraction_digit, ...
    'AccumWordLength',   max_digit, ...
    'AccumFracLength',   fraction_digit, ...
    'RoundMode',         'convergent', ...
    'OverflowMode',      'Saturate');
denormalize(Hd);


% [EOF]
